<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GottaGoRun"
        tools:targetApi="31">

        <!-- This is the new Login screen. It is now the LAUNCHER activity. -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- This is the new Register screen. -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- This is your original main screen. Notice the intent-filter is gone. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".RunHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".RunDetailActivity"
            android:exported="false" />

        <activity
            android:name=".PlannedRoutesActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAOI1Fdh--xmSPB41-wUv0h1b1BafzvjTE"/>
    </application>

</manifest>